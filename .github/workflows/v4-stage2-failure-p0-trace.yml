name: v4-stage2-failure-p0-trace

on:
  push:
    branches:
      - research/puct-v4-quiescence
    paths:
      - ".github/workflows/v4-stage2-failure-p0-trace.yml"
      - "src/benchmarks/v4-stage2-failure-p0-trace.ts"
  workflow_dispatch:

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      NODE_OPTIONS: --max-old-space-size=6144
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run typecheck
      - name: Trace V4=P0 reflected regressions
        run: >-
          npx tsx src/benchmarks/v4-stage2-failure-p0-trace.ts
          --out v4-stage2-failure-p0-trace.json
      - uses: actions/upload-artifact@v4
        with:
          name: v4-stage2-failure-p0-trace
          path: v4-stage2-failure-p0-trace.json
          retention-days: 7
